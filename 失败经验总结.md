# 失败经验总结

## 1. ES Modules 环境下的特殊全局变量 ReferenceError
- **问题描述**：在运行后端 Express 服务 `tsx server.ts` 时，出现了 `ReferenceError: __dirname is not defined in ES module scope` 崩溃。
- **失败原因**：因为项目在 `package.json` 中配置了 `"type": "module"`，整个项目属于 ES Modules 模式，在 ESM 中没有 CommonJS 的 `__dirname` 和 `__filename` 全局变量。
- **解决方案**：引入 Node.js 的 `url` 模块，通过 `fileURLToPath` 与 `import.meta.url` 动态重构获取当前文件的物理路径：
  ```typescript
  import { fileURLToPath } from 'url';
  const __filename = fileURLToPath(import.meta.url);
  const __dirname = path.dirname(__filename);
  ```
  在之后的 Node 后端开发中，若使用 `"type": "module"` 必须在初始化时提前处理 ESM 全局变量兼容性。

## 2. React 19/TS 5 严格的 JSX 属性校验错误
- **问题描述**：在进行前端 TypeScript 类型检测时，`History.tsx` 中向自定义组件 `HistoryCard` 传递 `key` 属性会触发编译错误 `Property 'key' does not exist on type...`。
- **失败原因**：在严格类型模式下，如果未声明或继承正确的 React Component 属性签名，TS 编译器会强制要求在 Props 类型定义中包含传入的特殊组件属性。
- **解决方案**：在 `HistoryCard` 的 Props 类型定义中显式加入 `key?: string;` 以兼容编译器校验规则：
  ```typescript
  function HistoryCard({ record, onSelect }: { key?: string; record: AnimalRecord; onSelect: () => void; })
  ```

## 3. Playwright 浏览器模拟测试中由于 CSS 动画导致的点击超时错误
- **问题描述**：在执行 Playwright 自动化浏览器测试时，`locator.click()` 抛出 `TimeoutError`（等待元素可见、启用并稳定超时），测试任务失败。
- **失败原因**：由于前端按钮设计了动态 CSS 动画效果（如 `animate-pulse-ring` 脉冲光环动画），Playwright 默认在点击前会执行“可交互性及稳定性检测（element stability check）”。因为动画使元素物理几何状态或视觉特征处于非静止状态，Playwright 误判定其为“不稳定元素”，从而无限期挂起直至超时。
- **解决方案**：在对带有 CSS 过渡或持续循环动画的元素执行 click 时，显式传入 `{ force: true }` 选项强制点击，以跳过 Playwright 的稳定状态前置检查，从而让浏览器自动化点击能够以极高的稳定性通过：
  ```javascript
  await startButton.click({ force: true });
  ```

## 4. 百度 AI 图像识别接口的参数格式与编码限制
- **问题描述**：在对接百度 AI 开放平台“动物识别”API 时，接口报错 `216201`（image format error）或识别出其他完全无关的噪声数据。
- **失败原因**：
  1. 百度的图像识别接口要求传入的 Base64 图片数据必须剔除掉 MIME 头部（例如 `data:image/png;base64,`）的前缀，仅保留纯密文内容。
  2. 接口的 Content-Type 强制要求为 `application/x-www-form-urlencoded`。在传输时，如果不对 Base64 数据中的特殊字符（如 `+`、`/`、`=`）进行标准的 urlencode，特殊字符会被后端解析器误处理（如 `+` 被视为空格），从而导致图片包损坏。
- **解决方案**：在 Express 后端对 Base64 数据进行前缀剔除，并使用 `URLSearchParams` 进行传参，它会自动进行标准的 `application/x-www-form-urlencoded` 字符转义：
  ```typescript
  const base64Data = image.replace(/^data:image\/\w+;base64,/, '');
  const bodyParams = new URLSearchParams();
  bodyParams.append('image', base64Data); // 会被 URL 编码
  ```

## 5. Supabase 默认启用的 RLS (行级安全策略) 阻碍数据匿名读写
- **问题描述**：在配置了正确的 Supabase 连通变量后，拍照识别仍无法正常保存历史记录，后端日志频繁报错：`new row violates row-level security policy for table "records"`。
- **失败原因**：Supabase 控制台在创建新数据表时，默认会对该表启用 RLS (Row-Level Security)。在没有专门创建并启用允许 anonymous (匿名) 用户插入和查询的安全策略（Policy）时，匿名 API 客户端的读写操作都会被直接拒绝。
- **解决方案**：
  1. 在 `supabase_schema.sql` 脚本尾部增加了关闭该表 RLS 的指令，引导用户去控制台/SQL Editor 运行以解除锁定：
     `alter table records disable row level security;`
  2. 重构后端获取历史和统计 API，对 Supabase 数据库错误进行 `try-catch` 拦截，即使报错也优雅地降级从本地内存拉取数据，并与数据库结果进行去重合并，保障全链路的高可用。

## 6. 超大 Base64 数据字段直接落库导致数据库报错或查询卡死
- **问题描述**：在用户上传原图或大图时，生成的几 MB 级别的 Base64 数据直接塞入 `image_url` 字段，会导致 Supabase 写入频繁超时，或因超出单次 Payload 传输上限而拒绝写入；在拉取发现历史时，由于数据体积极大导致接口严重卡顿。
- **失败原因**：数据库的文本字段直接保存巨量多媒体数据会导致严重的网络传输负载与数据库 IO 超标。
- **解决方案**：在后端设计「防暴轻量化过滤」机制。若无百科图片 URL，优先匹配本地辞典及 Mock 池中的高清公网 URL 进行替换；若无法匹配且原图 Base64 超过 150KB 时，自动采用风景大自然图片 URL 占位写入，彻底保障数据库读写的轻盈和极速秒开。

## 7. Playwright 浏览器自动化测试中由于硬编码延时导致的快照状态偏差
- **问题描述**：在对识别结果页进行快照抓取时，有时生成的 `03_result.png` 依然滞留在“正在智能分析中...”的相机画面，而不是跳转后的结果画面，但测试用例又通过了。
- **失败原因**：测试脚本使用了固定时长（如 `await page.waitForTimeout(4000)`）来等待异步接口返回和页面路由跳转。当后台数据加载或 API 响应在某次执行中产生细微延时波动时，固定等待时长不足以支撑跳转完毕便急于执行截图，从而导致截取到的并非预期的最终结果态画面，引发测试快照偏差。
- **解决方案**：摒弃不稳定的硬编码固定时间等待，改用基于特征元素的动态等待。例如，在跳转至结果页后，强制使用 `await page.waitForSelector('text=识别成功')` 来侦听识别结果的关键组件。当该元素被捕获时，表明页面数据已经完全渲染到位，此时再截图能 100% 保证快照的准确 and 时序一致性，同时缩短不必要的等待时长。

## 8. 对含糊反馈语境的语义识别偏差及未做逻辑比对便动手重构的失误
- **问题描述**：将用户在上一轮反馈的“识别动物后返回的信息，不显示图片”误解为“删除图片以满足无图版需求”。
- **失败原因**：在汉语自然语境中，“...，不显示图片”既可能是“这里不应该显示图片”的指令，也可能是一个 Bug 的陈述（“识别成功返回的数据里，图片竟然显示不出来/被实拍图挡住”）。未深入阅读现有逻辑（原逻辑 `localImage || activeRecord.image_url` 强行阻碍了后端返回的标准百科大图展示，导致详情页始终是不佳的实拍占位照甚至空白）就盲目对页面进行大刀阔斧的无图化重构，属于重大理解失误。
- **解决方案**：遇到存在多重理解可能的简短指令时，必须优先检查当前系统的代码逻辑与缺陷现象。若是程序自身原因造成的内容遮蔽或展示不全，需定位为系统 Bug 进行精准修复（如将详情页主图逻辑调整为优先读取 `activeRecord.image_url`，并增加“实拍参考”区域），同时在开发或迭代决策中保留严谨的语境推演机制。

## 9. 忽视边缘场景下用户拍摄 Base64 数据对主图字段污染产生的连锁 Bug
- **问题描述**：用户发现，虽然在识别结果页调整了图片加载逻辑，但在历史列表卡片以及详情页中，部分新识别物种的图像依然没有显示从百度或百科词典中调取的标准可爱动物照片，而是仍然渲染了用户拍下的低质量/乱拍照片。
- **失败原因**：在之前的性能优化中，为了保证在没有词典和百科配图时系统能保底保存，后端 `server.ts` 写有 `image.length < 150KB` 自动将实拍 Base64 缩略图（因为压缩后只有 10~20KB）直接赋给 `image_url` 并写入数据库的保底逻辑。因为数据库 `image_url` 被实拍的 Base64 图片污染了，导致前端渲染 `record.image_url` 时在卡片和详情页里依然展示了实拍图。
- **解决方案**：在后端落库时，强制建立物理层面的“标准百科配图隔离”。当遇到生僻物种确实没有百科大图时，禁止将用户的 Base64 填入 `image_url`，而是指定一个可爱的高清动物通用标准照（如 Unsplash 上的高画质萌宠幼狮）进行写入。这既能保障云端数据库无 Base64 存储的秒开性能，又确保了全局发现卡片 and 详情主图的动物相片皆为可爱高标准的萌宠百科图。

## 10. 第三方外部图片源防盗链策略导致的前端裂图 Bug
- **问题描述**：在实拍动物识别后，详情页面上的动物百科图片显示不出来，直接变成了一个写着“美国硬毛猫”字样和裂图标志的白色空框。
- **失败原因**：由于系统调取了百度百科等外部大型网站的图片链接，这些服务器默认部署了极其严格的 Referer 防盗链监测。当用户的浏览器发送带有 `Referer: http://localhost:3000` 的网络请求获取该图片时，百度的图片服务器会直接返回 403 Forbidden 拦截，从而导致前端 `<img>` 标签加载图片失败，呈现实拍后图片不显示的裂图故障。
- **解决方案**：
  1. 破除防盗链：在前端渲染外部网络图片的 `<img>` 标签中加入 `referrerPolicy="no-referrer"` 属性，指示浏览器在发送请求时不携带 Referer 头，从而绕过其防盗链逻辑。
  2. 智能 OnError 降级兜底：在详情页中维护 `imgSrc` 动态状态，当发生图片拉取错误时自动触发 `onError` 事件，无感退回到本地原图 `localImage`（使用原图 base64 绝不会失效）；列表页卡片中也同步添加 `onError` 占位降级，确保 100% 不发生裂图。

## 11. Playwright 自动化测试在小视口下的物理遮挡与错位点击
- **问题描述**：在进行 E2E 浏览器自动化测试点击“管理控制台”选项行时，Playwright 发生超时，没有成功进入 Admin 页面，反而重复跳转进入了“历史”页面，引发了测试死锁。
- **失败原因**：在移动端视口（375x812）下，底部的 BottomNav 是固定定位 `fixed bottom-0 z-[60]`。当底部的“管理控制台”按钮渲染在页面底部时，物理位置上恰好与 BottomNav 的“历史”按钮在 Y 轴上重叠且被遮挡。当测试脚本使用 `locator.click({ force: true })` 进行强制模拟屏幕物理点击时，强行点击了该位置上的像素点，由于 BottomNav 处于上层，点击被 BottomNav 拦截，结果误触激活了“历史”Tab。
- **解决方案**：
  1. 升级组件：为需要定位的关键交互元素加上唯一的 `id`，使选择器精准定位。
  2. 绕过物理定位：在自动化测试中，将依赖于屏幕像素坐标模拟的 `click()` 动作升级为 `dispatchEvent('click')`。这会直接触发 DOM 原生点击事件，绕开了物理遮挡、z-index 覆盖和页面视口滚动不足的问题，使自动化测试的健壮性大大提高。

## 12. 悬浮固定定位组件对流式布局的物理遮挡与防蔽设计
- **问题描述**：在对首页（引导闪屏 Splash 页）渲染底部导航栏（BottomNav）时，闪屏原有的底部核心操作按钮（“立即开始”以及版权声明）刚好处于 BottomNav 固定栏的覆盖位置，直接被遮挡无法全部呈现，影响用户操作和页面第一印象。
- **失败原因**：在页面采用弹性盒子（`flex-col justify-between`）布局且利用 `mt-auto` 推起底部元素时，如果没有显式在容器底部加上适配固定浮动高度的 padding-bottom，流式布局的元素会直接铺满到视口底部。当 BottomNav 采用 `fixed bottom-0` 强行悬浮其上时，两者高度空间重叠，形成物理遮蔽。
- **解决方案**：在渲染任何存在固定悬浮/绝对定位底部栏（如 BottomNav，通常高度为 60px~80px）的页面容器中，必须全局设置与其高度相匹配的安全底部内边距（如 `pb-28` 或 `pb-[80px]`），从而将弹性流式布局中的所有子元素安全顶托至悬浮栏上方，确保整体页面交互空间可用且美观。

## 13. 第三方 AI 与识别接口返回简繁混杂及英文俗名导致展示不合规
- **问题描述**：在实拍动物识别返回的详情数据中，物种名字有时包含繁体字（例如“東北虎”或“小熊貓”），或者趣味科普、习性中夹杂了一些英文属性，破坏了国内简中用户的阅读体验与业务规范要求。
- **失败原因**：在调用百度、Gemini 等外部大型 AI 和多媒体识别接口时，未对模型输出或接口响应进行严格的简中规范限制，仅信赖其自然概率返回。一旦模型产生微小的概率漂移或识别词带有地方色彩，就会把繁体字和英文等不规范字段直接返回给客户端展示。
- **解决方案**：
  1. AI 提示词层面：在 Gemini 生成 Prompt 中加上强硬的“除拉丁学名外，其余必须且只能使用中文简体输出”等明确指令。
  2. 后端数据清洗层面：在落库和输出前，编写一个高效轻量的简繁拼写清洗过滤函数 `convertToSimplifiedChinese`，结合精确映射纠正词典，过滤掉可能残余的繁体单字或常见生僻字，构筑接口输出端 100% 中文简体的最终安全网。

## 14. 隐藏的 API 字段调用漏传引发的数据多端割裂与测试超时 Bug
- **问题描述**：在 E2E 浏览器自动化测试的最后一个步骤中，页面在“历史记录”页中无法渲染出新识别的记录，导致 Playwright 找不到删除记录按钮而发生 30 秒超时错误。
- **失败原因**：前端 `CameraScan.tsx` 页面在调用 `identifyAnimal` 时，漏传了 `userId` 和 `userName` 两个可选的属性。虽然接口设计了默认兜底值，但当前端随机生成了不一致的个性化 `userId` 后，上传的记录由于 userId 割裂（归属于兜底 ID 8942371），而前端查询历史时过滤了当前随机 ID，从而导致新识别的记录在历史记录页无法展示。
- **解决方案**：在开发和重构接口参数时，必须检查每一个接口调用上下文的入参匹配。在 `CameraScan.tsx` 触发识别时，正确从本地 `localStorage` 读取 `userId` 与 `userName` 并填入接口请求中，实现识别上传与历史展现的完美联动。

## 15. 本地轻量化存储 LocalStorage 对 Base64 超大头像存储的配额溢出与溢出预防
- **问题描述**：在为用户端前台提供头像上传与持久化时，若是直接把用户选取的几十 MB 的高清原图 Base64 直接存入 `localStorage`，会导致浏览器抛出 `QuotaExceededError` (超出存储容量限制) 异常而引发前端崩溃。
- **失败原因**：`localStorage` 的总存储大小仅限制在 5MB 左右。一个几 MB 级别的 Base64 原图会非常轻易地挤爆该空间配额。
- **解决方案**：引入前端图像二次压缩机制。在用户选择头像图片后，利用 HTML5 Canvas 限制其最长边为 120 像素，并在 `toDataURL` 输出时压缩为 0.7 质量的 JPEG 格式 Base64。这能保证单个头像数据体积严格控制在 8KB~10KB 左右，既完美防止配额溢出，又大幅节省了网络读取和渲染时间。

## 16. 单页面应用（SPA）中多组件多实例的状态同步延时与事件同步机制
- **问题描述**：在个人中心（Profile）成功修改并保存头像后，切换到相机页面、历史页面以及识别结果页时，右上角的头像依然呈原默认头像，发生了跨组件状态不同步的延迟 Bug。
- **失败原因**：各组件为了读取头像均在内部使用了挂载时才初始化的 `useState` 状态。一旦其中一个组件（Profile）对 `localStorage` 的 avatar 键做出了写改动，由于 React 单向数据流且这些组件之间并非直系父子关系，其他已挂载的组件实例在内存中保留的旧 state 无法得到即时通知，只能等页面完全重新载入或刷新时才生效。
- **解决方案**：设计事件驱动型的跨组件零延时同步方案。在 Profile 组件成功修改头像并写入 `localStorage` 后，通过 `window.dispatchEvent(new Event('animdex_avatar_updated'))` 派发全局自定义事件；在 `CameraScan`、`MatchResult`、`History` 等各个组件中，通过 `useEffect` 挂载对该事件的侦听器，捕获后原地更新组件自身的 avatar 状态。这消除了跨页面同步的割裂，实现了零延迟、极速同步的高级体验。

## 17. 自动化测试中全局选择器冲突与唯一 ID 定位器的稳健性
- **问题描述**：在重构用户名修改为弹窗后，原来在 `browser_test.js` 中的测试选择器（例如 `input[type="text"]` 或 `button:has-text("保存")`）由于弹窗 DOM 结构的新增或者其它页面 input、button 元素冲突而变得非常脆弱，可能导致定位失效或误点击。
- **失败原因**：在页面元素增多时，流式 CSS 类型选择器（如单纯的 `input`）极其容易在页面挂载了 Modal 或其他复合组件时发生命名冲突，导致 Playwright 选择到非目标元素。
- **解决方案**：针对一切核心的前后端测试交互节点（如修改框、确定修改按钮、隐藏上传 input 等），在 React 组件内显式声明唯一的 `id`（如 `id="edit-username-input"` 与 `id="confirm-save-name-btn"`）。在 Playwright 脚本中改用精确的 ID 选择器进行操作，能够彻底解决界面重构带来的测试代码崩溃隐患，提升自动化回归的健壮性。

## 18. 自定义交互 Modal 对基于原生对话框 E2E 拦截测试的物理阻断
- **问题描述**：将前后台删除历史记录的原生确认对话框（`window.confirm`）重构为自定义网页 Modal 弹窗后，原本在 E2E 测试中通过侦听 `page.on('dialog')` 事件自动放行删除操作的机制不再触发，导致测试流程在等待删除元素消失时挂起并超时失败。
- **失败原因**：Playwright 的 `page.on('dialog')` 只能拦截浏览器底层的 `alert`、`confirm` 和 `prompt` 等系统级别弹窗。一旦修改为网页 DOM 级别的自定义遮罩 Dialog，该事件不会触发，测试代码需要像普通网页元素交互一样显式等待并点击 Modal 里的确定/取消按钮。
- **解决方案**：
  1. 为自定义网页弹窗的确认按钮加上唯一的特定 ID（如 `id="confirm-delete-btn"`），方便测试定位。
  2. 在测试脚本中，将删除按钮点击后的逻辑更新为：在点击垃圾桶按钮后等待 1 秒（动画过渡），使用定位器获取自定义弹窗的确定按钮并派发点击事件（`dispatchEvent('click')`），然后再等待数据物理删除并更新 DOM，确保测试链路无缝通过。

## 19. 冗余 Mock 数据及硬编码全局兜底图引发的“名不对图”的常识性 Bug
- **问题描述**：用户在使用拍照识物功能后，发现识别出来的特定动物（如“独角仙”）在详情页及历史卡片中展示的却是一张蝴蝶的配图；并且在网络环境较差或者网络拦截触发图片加载失败降级后，不论实际识别的动物是鸟类还是昆虫，前端都会展现一张狗狗的图片。
- **失败原因**：
  1. 后端词典数据（`fallbackAnimals` 和 `localAnimalEncyclopedia`）在录入时由于手误复制，将独角仙的 `image_url` 指向了黑脉金斑蝶的图片，导致在数据生成层面产生错误。
  2. 前端在处理 `img.onError` 异常降级时，由于思维定势，使用了统一的硬编码狗狗图片地址，未能结合当前被渲染动物的门类进行动态映射展示，从而导致兜底展示与实际物种不符。
- **解决方案**：
  1. 纠正错误配置：对后端词典中发生重合或错误的动物大图 URL 进行全面排查，将独角仙修正为 Wikimedia 上公共且免除防盗链的高清雄虫照片。
  2. 智能分类降级：在前端构建通用的 `categoryFallbackImages` 分类兜底大图池（哺乳类-狗、鸟类-鹦鹉、昆虫-瓢虫、其他-绿头龟），并在各页面的 `onError` 降级处理器及 `imgSrc` 初始赋值中，通过 `record.category` 属性动态获取对应门类的大图。这从机制上完全封堵了名图不符的常识性 Bug，保障了系统的高可用视觉自愈能力。

## 20. 强依赖特定局域/区域性 CDN 的外部资源导致的前端降级失真连锁 Bug
- **问题描述**：在特定的本地域名或无梯网络环境下，无论识别什么哺乳动物（如小熊猫、家猫、东北虎），界面上的大图均显示为同一只可爱的比格犬，引发了“数据调取错误、显示单一狗狗图”的恶劣体验。
- **失败原因**：
  1. 系统内置的动物百科链接使用的是 Google AIDA 的存储地址（`lh3.googleusercontent.com`）。该域名在部分网络环境下被屏蔽或不可达，从而在加载时 100% 发生网络连接超时或裂图报错，触发前端 `img.onError` 事件。
  2. 触发降级后，由于这些物种同属于“哺乳类”，前端在进行自适应降级时，直接将图片源赋为了 `'哺乳类'` 的唯一兜底图（即那只比格犬的 Unsplash 链接）。因为主图 100% 加载失败，所以全部被降级渲染为了相同的比格犬图。
- **解决方案**：
  1. 全面去 Google 化：将所有在 Mock、拟真兜底以及本地词典中以 `lh3.googleusercontent.com` 开头的外部图片资源全部清洗并替换为全球高连通性、无跨域和跨网限制的 Unsplash 经典图片源，彻底斩断裂图隐患。
  2. 保持链路畅通：保证原始百科图能 100% 成功请求并渲染，确保降级分支（显示狗狗）仅在极端断网或链路物理切断时起防御作用，真正恢复了多动物图片的各自唯一呈递。

## 21. 页面跳转与路由切换的异步时序覆盖冲突
- **问题描述**：在 E2E 自动化测试中，点击闪屏（Splash）页的"立即开始"后，脚本紧接着点击底部导航的"相机"页卡进行下一步测试。此时，页面会突发卡死在历史页的骨架屏状态而无法完成识别测试。
- **失败原因**：在闪屏组件 `Splash.tsx` 中，用户点击"立即开始"会调用 `handleStart`，该方法内部通过 `setTimeout` 延迟 2000 毫秒才触发 `onStart()` 跳转路由。如果 E2E 测试脚本的等待时间不够（如只等待了 1500 毫秒），在第 1.5 秒时发送点击事件触发了 App 中的 `setCurrentScreen('camera')` 状态更改，但在第 2.0 秒时 `Splash` 的 `setTimeout` 回调触发，将路由强制重写覆盖为默认的 `'history'`，导致发生不可预知的时序重写覆盖冲突。
- **解决方案**：在测试用例中对带有延时转换的组件执行测试时，必须留出充足的缓冲等待时间（如将等待时间延长至 3000 毫秒），避开其内部的定时器结算，或在测试中严格监听页面状态改变后再触发后置操作，以确保异步时序的绝对稳健。

## 22. 云数据库物理延迟导致的数据加载卡顿与 SWR 客户端缓存秒开优化
- **问题描述**：在多级单页路由之间切换时，由于每一次切换页面都会触发一次针对 Supabase 云端数据库 API 的请求，加载过程需要耗费数百毫秒乃至一秒，造成页面频繁出现骨架屏与白屏卡顿，体验不够流畅。
- **失败原因**：Supabase 等云端数据库受限于网络物理延迟 and 查询损耗，直接发起网络请求无法做到“秒开”。
- **解决方案**：在客户端引入 SWR（Stale-While-Revalidate）思想。在组件外层声明持久化的全局数据缓存变量。当组件挂载时，优先读取该缓存数据进行首屏渲染，随后再在后台静默发起 fetch 动作拉取最新数据并更新缓存和状态。这兼顾了秒级载入与数据时效性。同时，对于不需要防抖等待的常规挂载与分类切换（非主动输入搜索词），必须果断移除 300ms 的 debounce 防抖延迟，让请求能即时发出，将交互延迟降到最低。

## 23. 外部数据库存量失效链接引发的批量降级“名不对图”Bug
- **问题描述**：在修复了前端降级和新识别录入图片的规则后，前台历史记录列表中依然出现了除名字外所有动物的配图均是同一张狗狗图片的重大常识性 Bug。
- **失败原因**：虽然在新代码里拦截并保障了新写入的数据连通，本意上忽略了云端 Supabase 数据库中原本就存在的大量早期历史遗留脏数据。由于这些历史记录的 `image_url` 依然包含国内不可达的 Google 百科图片链接，导致页面在加载时 100% 触发 `img.onError`。因为这些动物同属于 `哺乳类`，因此都被迫重定向降级为了哺乳动物的统一兜底大图（即那只狗狗图），看起来就是所有内容都显示同一张默认图。
- **解决方案**：针对此类环境相关的脏数据污染问题，必须采取“一键脚本存量清洗 + 接口热自愈清洗”的双防线设计：
  1. 编写独立脚本连通数据库，一次性洗净全部存量失效数据，将已坏掉的链接替换为对应物种在公网通畅的高清大图。
  2. 在后端历史拉取 API 中增加过滤逻辑，在将数据发送给前端前执行动态自愈，将已有的或未被脚本覆盖的脏图片自动映射为最新的百科大图，从而提供稳健 of 防裂自愈能力，恢复数据的多样与准确性。

## 24. PC 端大屏拉伸失真与 fixed 元素在子容器内定位受限的限制
- **问题描述**：为了让针对手机设计的移动端图鉴 App 在 PC 宽屏浏览器上更精致美观，我们需要将其宽度限制为 420px 黄金尺寸并水平居中。但直接在容器上设置最大宽度后，页面中所有使用 `fixed` 定位的顶栏（Header）、底栏（BottomNav）和全局 Toast 依然相对于浏览器视口定位，直接横向拉满整个屏幕，破坏了界面的结构性。
- **失败原因**：在标准 CSS 规范中，`position: fixed` 元素的包含块（Containing Block）默认始终为浏览器视口（Viewport）。直接限制父容器 `#root` 的宽度对 `fixed` 定位子元素无效。
- **解决方案**：
  1. 在父容器 `#root` 上设置 `transform: translate3d(0, 0, 0)` 属性。这会强制创建新的层叠上下文，并将所有子级 `fixed` 元素的定位包含块（Containing Block）重定向到该父容器，使得 `fixed` 元素能够像 `absolute` 一样紧密依附于 420px 的父容器内.
  2. 使用媒体查询限制，当屏幕宽度小于等于 420px（即在真实手机或超窄屏幕上），自动移除阴影与细边框，实现物理平滑贴合，从而提供最佳的跨端美学体验。

## 25. 系统迭代中冗余未上线业务的精简及与 E2E 回归测试的无感解耦
- **问题描述**：在个人中心（My / Profile）菜单中包含了一些并未完全上线或与现有页面存在大面积重复的选项（如“收藏的物种”与“成就中心”）。直接将其物理删除时，需要防范可能带来的前端样式崩溃以及 E2E 回归测试用例中由于元素定位失败引发的报错。
- **失败原因**：如果在编写 E2E 测试脚本时，过度依赖在页面中查找特定的菜单文本或未做充分 of 解耦，直接删除菜单项可能会导致脚本中包含的 “waitForSelector” 或 “click” 事件挂起并超时。
- **解决方案**：
  1. 在重构和清理业务逻辑时，必须率先核对 E2E 测试用例脚本（如 `browser_test.js`），确保测试主干流程中并未对这些即将被裁撤的次要/未上线元素有强物理依赖。
  2. 保持测试脚本与核心组件的充分解耦，使非主干交互菜单可以安全物理下线，同时通过回归测试验证精简后的界面截图表现，降低维护成本。

## 26. 渐进式多轮业务重构裁剪中废弃 HTML 容器的处理
- **问题描述**：在经历了多轮将个人中心（My / Profile）菜单项进行物理下线和业务缩减的过程中，原先包含“我的图鉴”与“探索地图”的第一组 HTML 容器（`div`）在内部元素清空后，如果不做物理移除，会在页面渲染时留下一个多余空置的阴影卡片边框，严重影响页面的视觉档次。
- **失败原因**：在剔除子组件或子元素时，忽略了包裹它们的父容器（如带圆角、背景色和 shadow 的 `div`），在子节点完全被删去后，父容器本身仍在流式布局中占据一定高度并展示样式。
- **解决方案**：在开发和重构菜单裁剪时，当某个功能块容器中的所有子菜单都被彻底移除时，必须连同该包裹容器的 `div` 元素及其注释一并彻底物理删除，防止残余空 DOM 结构产生的废弃样式，以维护 premium 级的系统设计美学。

## 27. 移动端/手机宽度限制下网格大图卡片布局的笨重失真
- **问题描述**：在对系统应用进行 `420px` 手机宽度限制并居中悬浮后，历史记录页面中原有的“首张 16:9 巨型大卡片 + 其它单列 4:3 大卡片”的网格向下流式堆叠布局会显得极其臃肿、笨重。每个卡片由于在纵向占用了巨大的高度，导致首屏仅能显示一到两个记录，且信息密度极低，给用户带来严重的翻阅疲劳。
- **失败原因**：原本为大宽屏或平板设计的网格卡片在缩窄到单列后，高宽比导致纵向物理尺寸过大，完全不符合手机端原生 App 紧凑、小巧、高效的列表交互习惯。
- **解决方案**：针对在窄屏/手机模式下的图鉴列表，应果断摒弃传统的全宽大图卡片模式，升级为“左图右文”的 List row 结构：
  1. 左侧利用固定宽高比的正方形小图片（如 `w-24 h-24`）做局部精细大图聚焦，右侧采用纵向 Flex 弹性盒依次排列高密度信息（标题、学名、彩色状态小药丸、紧凑日期及小操作按钮）。
  2. 保持删除等动作按钮的 HTML 属性（如 `title="删除记录"`）不变，使重构样式在保持高级质感的同时，对已有的 Playwright E2E 自动化测试脚本保持 100% 无缝兼容和透明。

## 28. 百科配图空缺时粗暴兜底引发的跨物种覆盖 Bug 与实拍优先防御
- **问题描述**：在拍照识别时，如果上传猫的图片且识别为猫，结果详情页的大图展现出的却是一只比格犬，且再次上传也无法更正，引发了严重的逻辑错误体验。
- **失败原因**：
  1. 后端模糊匹配设计过于简单（仅使用 raw 词包含校验）。在识别出“波斯猫”或“美国硬毛猫”等未直接录入本地词典的生僻具体猫时，由于无法与词典中的“家猫”形成字词整包包含，导致匹配失败；而它们同属于哺乳类，在后端最终都被强行指派了“哺乳类”的兜底占位图（即那只比格犬）。
  2. 前端在结果详情页渲染主图时，写有 `activeRecord.image_url || localImage` 的逻辑。由于后端即使在兜底时也给 `image_url` 赋了非空的狗狗占位图，导致 `activeRecord.image_url` 恒有值，使用户拍摄的真实的猫的 `localImage` 永远被遮挡无法显示，强制变成了显示狗。
- **解决方案**：
  1. 建立后端核心字级关联词映射（如只要名字包含“猫”就自动指向词典的“家猫”高清猫图，含“犬/狗”指向“比格犬”），提高了类属百科图的命中能力。
  2. 前端重构主图显示优先级：对图片链接进行识别，如果发现接口返回的实际上是分类兜底大图之一，且用户存在真实的实拍照（引入长度检测避开一像素测试占位图），则强制降级百科图，优先选择并呈现用户的实拍照，从而在根本上提供了物种特性的精确展示和防御。

## 29. 取消引导页等主路由变更导致的 E2E 自动化测试用例雪崩
- **问题描述**：在彻底取消引导闪屏页（Splash Screen）并直接以历史记录页面为默认首页时，E2E 自动化回归测试执行时发生了严重的超时挂起和雪崩报错。
- **失败原因**：重构代码删除了 `currentScreen === 'splash'` 的路由和 `Splash` 组件，导致闪屏页面的 DOM 元素以及“立即开始”按钮不复存在。然而自动化测试脚本仍强依赖旧的测试步骤，试图在首屏加载后去等待并点击该不存在的按钮，这导致 Playwright 查找该元素直至超时崩溃，中断了后续所有的测试步骤。
- **解决方案**：在对涉及核心初始化路由或主页面的物理重构时，测试脚本必须同步更新。必须剔除原先对已下线页面（如 Splash 页）的所有定位、等待和点击动作，重新校准为直接对新默认首页（历史页面）进行截图和状态验证，并紧接后续流程，确保自动化回归测试能够 100% 成功。

## 30. Playwright 自动化测试中使用 networkidle 等待状态导致的偶发性超时
- **问题描述**：在执行 E2E 浏览器自动化测试时，有时在 `page.goto` 访问本地页面时会抛出 `TimeoutError` 超时异常，导致测试整体崩溃挂起。
- **失败原因**：在 Playwright 脚本中使用了 `{ waitUntil: 'networkidle' }` 参数，要求必须在 500ms 内没有任何网络活动才视为加载完成。然而在真实的项目环境下，页面中经常会异步拉取高延迟的外部网络资源（如 Unsplash 动物配图、地图资源、外网字体等），或因接口报错不断重试产生网络背景流，导致网络状态无法在 30 秒内完全归零，从而引发测试超时雪崩。
- **解决方案**：在页面跳转 and 初始化阶段，放弃强依赖 `networkidle`，将其调整为更安全、连通率更高的 `{ waitUntil: 'load' }` 级别，并配合 `page.waitForSelector` 对我们所关心的页面特定常驻元素进行动态监听。这极大地保证了自动化测试的时序健壮性，杜绝了网络延迟带来的雪崩。

## 31. 日志记录中超长 Base64 行内截断引发的代码破损与拼图自愈机制
- **问题描述**：在进行代码 Compaction 备份或多重非连续替换（`multi_replace_file_content`）时，由于文件内包含超长的 Base64 编码或极其冗长的图片 URL，导致日志存储系统为了防止溢出而强行将该行内容进行物理截断并填充为 `<truncated X bytes>`。再次从历史提取或替换该代码段时，会导致单双引号未闭合或代码块彻底缺失，引发项目无法通过编译的严重 Bug。
- **失败原因**：日志系统对单个 API 请求和工具调用的 arguments 进行了最大体积限制。当单行代码或超长字符串的字节数过大时，在系统后台保存的 `transcript.jsonl` 中，该部分数据会被替换为 truncated 占位符，如果盲目恢复会丢失数据。
- **解决方案**：
  1. **资源轻量化隔离**：绝对不要在前后端源文件内硬编码超长的 Base64 图片密文或包含多重查询参数的复杂 CDN 长链接，统一采用简短的 Unsplash 短链，让源文件保持轻量化。
  2. **自愈拼图算法**：如果本地文件不幸被 truncated 破坏，可以通过 Python 脚本读取原始 `transcript.jsonl` 中 `VIEW_FILE` 时段所显示的完整行段，将没有被 truncation 污染的干净代码块依次按照 line_number 进行行拼接重组，部分缺失的短字利用邻近上下文手动复原，可以达到 100% 物理自愈。

## 32. 接口路径拼写不匹配导致的局部请求 404 及前台获取数据失败
- **问题描述**：在进入“我的”个人中心和管理后台页面时，页面频发弹出“获取个人信息失败”的错误 Toast，虽然自动化测试可以通过但有明显的报错日志。
- **失败原因**：在开发和重构统计接口时，后端的 Express 路由路径被定义为了 `/api/stats`，但在前端 `api.ts` 工具函数的封装中，获取该统计的请求路径却被笔误写成了 `/api/profile`。两者由于拼写不匹配，导致前台请求该接口时直接触发 404 错误。
- **解决方案**：
  1. **前后端接口严格对齐**：将 `api.ts` 中请求的路径由 `/api/profile` 修正为正确的 `/api/stats`，以与后端路由完全保持一致。
  2. **全面审查控制台异常**：每次进行测试和截图验证时，不仅要看 E2E 测试脚本的退出状态，还要仔细审阅 `BROWSER_CONSOLE` 打印的日志信息，及早发现并清洗隐蔽的 404/500 等静态资源或接口获取异常，确保系统极致的高可用性。

## 33. 百科标准图与用户实拍照默认展示的一致性冲突及交互折中
- **问题描述**：用户发现，历史记录列表展示的是精美唯一的百科标准图，但点击进入详情页后，由于详情页默认自动展示了实拍大图，导致两边缩略图和主大图产生不一致，破坏了图鉴的统一美学。
- **失败原因**：在对实拍图的保存与回溯修复中，为了凸显用户拍照成果，详情页被强行默认渲染为 `showUserPhoto = true` 的实拍图模式。但列表页由于信息密度和排版限制，依旧展示百科标准图，这忽略了“默认加载第一视角的视觉统一”。
- **解决方案**：
  1. **以百科图为默认视觉基准**：在列表页与详情页初始化时，默认均一致展示精美的百科标准图（`record.image_url`），保证应用的首屏感官高档统一。
  2. **将实拍照保留为二级交互探索**：在详情页保留“看我拍的”切换按钮和“探索足迹相册”双图并排卡片。将实拍照定位为辅助对比与探索的互动元素，使用户默认看到极具张力的百科大图，同时又拥有无缝翻转回溯实拍照的能力，平衡了视觉美学与实拍记录功能。

## 34. 异步分析加载阶段相机取景器占位图的动态替换美学
- **问题描述**：在进行拍照识物或一键演示时，点击拍照/演示后，后台可能需要数秒时间进行 AI 智能解析。在这个加载等待阶段，虽然顶部显示了“正在智能分析中...”，但取景器背景依旧是初始的默认大熊猫图片，给人一种“图片没有成功提交/卡在上一状态”的死板感觉。
- **失败原因**：在组件初版设计中，取景器的 `<img>` 背景 `src` 被硬编码为了静态的默认大熊猫 URL，只在上传完成后才将新结果反馈到 MatchResult 组件，忽略了在“上传与解析中”这个过渡状态下对取景器背景的同步更新。
- **解决方案**：
  1. **状态化 Viewfinder 背景**：将取景器的背景图从静态的 URL 剥离，声明为 React 状态 `bgImage`，并在点击拍照、选择相册或点击 AI 演示的第一时间（即 `processImageIdentification` 之前）立即调用 `setBgImage` 进行实时大图覆盖。
  2. **去除高频干扰提示**：用户对于拍照到解析的过程已有心理预期，过度密集的 Toast 提示（如“正在压缩图片...”）会造成页面视觉抖动并降低产品的高级感。我们应当只保留一个静默加载态（如顶部常驻药丸框“正在智能分析中...”），从而在视觉与动效层达到最优雅的折中。

## 35. 后端兜底分类粗暴归档与缺乏全局清洗纠偏的常识性 Bug
- **问题描述**：在进行拍照物种识别时，很多明明属于哺乳动物或鸟类的动物（如斑马、赤狐、大雕鸮、柯基），被频繁地归类到了“其他”分类下，导致页面展示和分类统计出现常识性偏差。
- **失败原因**：
  1. 之前设计的 fallback 本地拟真逻辑中，只为包含本地词典 8 种核心关键字的动物做映射，而没有对更广泛的未知物种做科学归类，导致未知动物在 fallback 或是 Gemini 超额异常时一律以 default 值 `"其他"` 落库。
  2. 在接口最终向数据库写入 record 时，完全没有设立终极 of 分类安全审核机制，放任不规范或错误的分类信息录入数据库。
- **解决方案**：
  1. **部署全局终极分类自愈过滤机制**：在 record 物理入库 of 最后关头，对 `category` 字段强加校验。如发现其为“其他”或不合规字符，自动基于本地百科精准匹配和多达数十个大范围物种特征字（猫、犬、鸟、雕、虫、蝶等）进行自愈纠偏，确保物理落库的数据 100% 科学正确。
  2. **云数据库存量纠错清洗**：除了修正增量，必须针对存量开展彻底洗涤。通过编写专门的数据库 CommonJS 清洗脚本，将历史记录中由于兜底 Bug 产生的存量数据进行一次性重映射和更新，真正完成业务闭环。

## 36. 后端数据加工与前端文案属性不匹配导致的描述内容重复单一问题
- **问题描述**：在拍照识别一些非核心词典的生僻动物时，详情页中原本标为“趣味科普”的地方，内容在多次测试下表现为完全相同、千篇一律的一句话，显得产品粗糙且无深度。
- **失败原因**：
  1. 在后端兜底 fallback 逻辑中，由于 Gemini 请求超限报错，程序将非核心动物的 `fun_fact` 属性一律设为了默认的静态中文字符串，而没有积极引用百度 API 已经拉取到的专属 `baikeDesc`（百科描述）字段。
  2. 前端展示标题“趣味科普”与数据本身所能提供的高质量“简要介绍”在词义上产生了割裂，且没有任何针对长度保护的切片（slice）拦截器，存在超长文本破坏排版美学的风险。
- **解决方案**：
  1. **数据源联动与强力裁剪限制**：在 `server.ts` 降级兜底中，优先读取和填充百度的 `baikeDesc` 字段，并在生成 record 的最终环节点，添加 `.slice(0, 150)` 彻底锁死其字数在 150 字内。
  2. **前后端文案一致性升级**：将前端 `MatchResult.tsx` 中的展示 Label 更名为“简要介绍”，并改用更加商务和精简的 icon，确保即使在网络故障降级下，也能为用户带去真实、精简的该动物专属百科简介。

## 37. 前端按钮文案微调与 E2E 测试定位器、本地 Mock 数据的多端同步缺失
- **问题描述**：在将前端详情页“保存到收藏”按钮文案精简为“保存收藏”后，自动化测试脚本运行至点击该步骤时超时报错，且离线测试状态下的默认小熊猫和家猫 Mock 简介仍为较长故事或非真实简介。
- **失败原因**：
  1. 自动化 E2E 测试脚本（如 `browser_test.js`）强依赖按钮上的文本内容进行定位（`page.locator('button:has-text("...")')`）。前端按钮文字被修改，而测试脚本中的匹配词未同步修正，导致测试程序无法找到元素，最终超时挂起。
  2. 进行数据库洗库清洗时，仅关注并清洗了 Supabase 的云端存量记录，忽略了后端 `server.ts` 中定义的 `memoryRecords` 内存 Mock 队列以及前端 `MatchResult.tsx` 自带的 `fallbackRecord` 数据。使得在离线、本地内存模式或初始兜底展示时，依然渲染出旧的超长和重复趣味长文，造成数据体验割裂。
- **解决方案**：
  1. **文案与 E2E 定位器双向对齐**：凡是修改前端与测试相关的可交互文本（如按钮文案、输入框 placeholder 等），必须第一时间全局搜索测试脚本，同步更新其文本匹配器，或尽可能使用更稳健的 ID 选择器进行定位。
  2. **云端与本地 Mock 同步重构**：任何数据格式或简介去重的重构，都必须同时覆盖“云端存量数据”、“本地内存 Mock 数组”以及“前端默认 fallback 结构”，实现全场景、全链路数据一致性。

## 38. Supabase 对 UUID 类型主键进行空字符串过滤匹配删除时触发的语法错误
- **问题描述**：在执行清空数据库 `records` 表所有历史记录时，直接使用 `.delete().neq('id', '')` 会导致数据库接口报错 `invalid input syntax for type uuid: ""`。
- **失败原因**：由于 `records` 表的 `id` 字段属于严格的 UUID 类型，Supabase/PostgreSQL 在进行条件判定时会对其右值的格式进行强制校验。因为空字符串 `''` 并不符合规范的 UUID 格式，数据库在查询预编译阶段就直接抛出语法不合规异常，导致物理删除动作失败。
- **解决方案**：
  1. **基于通配时间或合法UUID过滤**：当需要无条件清空整表数据时，为了绕过客户端无法使用空过滤器的保护机制，应选择非 UUID 的常驻字段进行过滤。例如在 `created_at`（时间戳类型）字段上过滤所有大于 1970 年的记录：`.delete().gt('created_at', '1970-01-01')`；或者是使用一个格式正确但不可能存在的 dummy uuid 字符串来匹配 `neq('id', '00000000-0000-0000-0000-000000000000')`。
  2. **提高底层报错敏感度**：对数据库底层异常要具备更敏锐的拦截意识，利用时间戳范围删除可作为各云数据库表清空的极佳兼容套路。

## 39. 静态占位伪地图图片加载失败的裂图隐患与高质感文字卡片性能重构
- **问题描述**：在详情页底部“发现足迹分布”中，初版使用一张 Unsplash 地图静态大图配合跳动图钉的方案，容易在网络缓慢或外部图片源失效时产生大面积裂图空白，且页面加载性能受影响。
- **失败原因**：在非强空间交互的地理信息展示中，硬编码引入静态外部大图，既没有真实的拖拽/缩放细节，还容易触发跨域、网络延迟与裂图隐患，降低了 App 的视觉档次与网络稳定性。
- **解决方案**：
  1. **采用高质感文字探索卡片替代**：将地图大图及定位点结构完全移除，替换为淡灰磨砂底色（`bg-gray-50/70`）与精细圆角的自适应文字信息容器。
  2. **注入灵动的呼吸微动画**：在指南针（`explore`）图标容器上应用 `animate-pulse` 微秒级呼吸动效，在降低 90% 以上图片传输网络开销的同时，保持并提升了界面的高级图鉴学术质感。

## 40. 前端业务裁剪与 DOM 容器残留导致的多余空边距与线条污染问题
- **问题描述**：在对详情页底部的“发现足迹分布”板块进行快速物理裁切删除时，若只剔除了内部的核心文字，会在详情页底部卡片留下多余的灰色分割横线与无谓的空白高度。
- **失败原因**：在组件开发中，只对被删除的部分进行定位清理，忽略了包裹它们的外层辅助容器样式（如 `<div className="border-t border-gray-100 pt-6">`）。在子节点被删去后，父级的 `border-t` 和 `pt-6` 依然保留，在流式卡片底部形成了一条不合规的灰色短线和空白高度。
- **解决方案**：
  1. **执行彻底的物理全包删除**：在任何多轮次的业务逻辑裁剪时，必须秉承“整体容器一并清除”的理念。必须连同分界线样式、外部包裹 `div` 容器与内部子孙节点做一次性物理切除。
  2. **加强 UI 细节审查**：在执行完删除并运行 Playwright 回归测试后，细致比对 `03_result.png` 中详情卡片底端的排版比例，确保无残留的灰色死线或不合逻辑的空白，保持系统的极致细节品质。

## 41. 物理地址残留污染引发的刷新路由倒流 Bug 与 pushState 改写清洗
- **问题描述**：在建立基于独立物理路径（如 `/admin`）的后台，当管理员点击返回前台的按钮后，浏览器地址栏中依然残存着 `/admin`。一旦此时在此状态下刷新浏览器，应用会重新读取地址栏并将其强制引导重入管理后台，产生了严重的路由逻辑倒流 Bug。
- **失败原因**：在单页面应用（SPA）中，利用状态变量进行页面导航仅仅是在内存中切换视图。在直连物理地址访问时，如果不采取手段清洗浏览器的真实地址栏，物理 URL 状态与内存中的前台路由状态将产生严重失谐，导致刷新时直接发生定位偏差。
- **解决方案**：
  1. **部署 URL 物理级改写清洗**：在返回前台及返回主页的点击事件中，必须配合 React 状态变量导航，同步显式调用 `window.history.pushState({}, '', '/')`。这能在单页不刷新的情况下，悄无声息地将浏览器地址栏净化为根目录 `/`，保障了多端刷新的一致性。
  2. **E2E 流程中显式包含页面重载测试**：在自动化测试中，将对于特定后台的直连直接写入 `page.goto`，并在退出后加入适当的延时检测，确保测试能充分覆盖前进后退、登录和退出的边界，保持极高的业务可靠性。

## 42. E2E 自动化测试约束下的高级 UI/UX 重构原则
- **问题描述**：对复杂前端页面（如个人中心）进行大规模美化、布局重构与互动模块添加时，若开发中不慎篡改了原有 DOM 节点的关键 ID 选择器或关键属性，会导致现有的 E2E 浏览器自动化回归测试大面积超时或死锁。
- **失败原因**：在追求高质感 UI 交互（例如磨砂玻璃卡片、等级经验条、打卡签到名言模态框）的重构中，开发人员非常容易为了使 DOM 结构更加清爽或为了使用 Tailwind / flex 布局，而随手修改、重命名或遗漏了之前开发阶段留下的测试定位标志（如修改头像的 `#avatar-upload-input`、修改昵称弹窗中的 `#edit-username-input` 或是保存的确定按钮 `#confirm-save-name-btn` 等）。测试工具找不到这些 DOM 元素便会超时挂起，破坏了回归测试的闭环。
- **解决方案**：
  1. **以测试定位锚点为最高重构指令**：在大规模重构前，必须完整阅读并提炼出 E2E 自动化测试脚本（如 `browser_test.js`）所涉及的所有元素寻址模式。在进行新页面美学布局和内容开发时，保证原有的 ID、class 和 title 属性以 100% 的准确率继承到新的 DOM 结构中。
  2. **新互动模块与测试环境的无缝共存**：添加如“签到信笺弹窗”、“勋章故事 Modal”和“个性签名”等复杂层叠模态框时，务必注意多重 Modal 的 z-index 遮挡关系，确保它们不会妨碍底层原本测试组件的模拟点击和文件上传行为。通过运行 `tsc --noEmit` 和自动化测试脚本进行全链路回归，在保障 100% 测试通过的前提下呈现 wow 的视觉设计。




## 43. 大模型结构化输出引发的参数“幻觉”覆写危机
- **问题描述**：在接入 Gemini 大语言模型丰富动物科普资料时，发现系统经常出现“动物数据正确，但展示了一张完全无关的图片（例如大熊猫配了一张纸袋里的家猫）”的灵异现象。
- **失败原因**：在向大模型请求返回结构化 JSON 时，Prompt 中包含了 `"image_url": "该动物的高清 Unsplash 真实标准配图 URL"`。大语言模型本质上是概率模型，它无法真正执行“去 Unsplash 搜索合适图片”的动作，而是会根据概率“幻觉”出一个看似合法的 URL（甚至随意拼凑）。而在后置逻辑中，我们优先采用了这段幻觉出来的 URL，从而意外覆盖了原本由百度百科 API 准确返回的正版动物科普图。此外，在后备词库匹配时，“熊猫”等长词排在“猫”之后，导致长短词冲突截胡（大熊猫命中了“猫”从而展示家猫图片）。
- **解决方案**：
  1. **收缩大模型指责边界**：在 Prompt 中彻底剔除 `image_url` 字段的请求，绝不让大语言模型去负责它不擅长的确切链接返回，只让它做纯文本信息的科普补充。
  2. **绝对信任权威数据源**：明确优先级，只要百度百科返回了合法的图片，就必须绝对保留，不得被后续的任何 AI 补充逻辑给覆盖。
  3. **数组遍历优先级排序**：在任何涉及 `find` 或 `some` 的字符串模糊匹配数组中，必须将长度更长、更具体的词汇（如“大熊猫”、“哈士奇”）排在短词汇（如“猫”、“狗”）之前，防止提前被泛化条件拦截。

## 44. Vercel 部署静态站点与 Express 接口的架构失配陷阱
- **问题描述**：将基于 Vite + Express 的全栈项目一键部署到 Vercel 后，前端页面加载正常，但所有数据交互失效，控制台抛出 `/api/*` 的 404 错误，用户误以为“数据库无法连接”。
- **失败原因**：Vercel 的默认部署流水线在没有明确配置的情况下，仅会执行前端构建（如 `npm run build`）并将 `dist` 目录托管为静态页面。项目中根目录下的 Express `server.ts` 被直接忽略且并未作为后台服务启动，导致所有的 API 请求都石沉大海。
- **解决方案**：
  1. **构建 Serverless 中转路由**：在项目根目录添加 `vercel.json`，配置 `rewrites` 将所有 `/api/(.*)` 的请求强制转发至特定的后台入口。
  2. **封装 Express 为 Serverless Function**：创建 `api/index.ts` 并在其中导入原本的 Express `app` 实例并 `export default app`。同时在原 `server.ts` 中加入环境判断，确保其在 Vercel 平台上运行时跳过 `app.listen` 操作，让 Vercel 引擎自行接管请求的生命周期。
